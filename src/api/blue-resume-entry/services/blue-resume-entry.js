'use strict';

/**
 * blue-resume-entry service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::blue-resume-entry.blue-resume-entry');
